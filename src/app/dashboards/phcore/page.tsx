import PhcoreDashboard from "@/components/dashboards/PhcoreDashboard";

export const metadata = { title: "PH Core Encounter Rate Report — PH eReferral" };

export default function PhcoreDashboardPage() {
  return <PhcoreDashboard />;
}
