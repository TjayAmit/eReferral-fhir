import LguDashboard from "@/components/dashboards/LguDashboard";

export const metadata = { title: "LGU Referral Dashboard — PH eReferral" };

export default function LguDashboardPage() {
  return <LguDashboard />;
}
